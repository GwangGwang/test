version: 2.1
orbs:
  go: circleci/go@1.7.3
jobs:
  build:
    docker:
      - image: google/cloud-sdk:411.0.0-alpine
    steps:
      - checkout
      - go/install:
          version: 1.21.0
            